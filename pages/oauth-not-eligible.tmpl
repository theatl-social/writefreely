{{define "head"}}<title>Membership not found &mdash; {{.SiteName}}</title>{{end}}
{{define "content"}}
		<div class="content-container tight">
			<h1>We couldn't find an active membership</h1>
			<p>This Mastodon account isn't currently linked to an active theATL.social membership. We double-checked just now, so if you recently joined or changed plans, this should catch up shortly on its own.</p>
			<p>If you believe this is a mistake, check your membership status at <a href="https://members.theatl.social/account">members.theatl.social</a>, or <a href="https://join.theatl.social/support">get in touch</a> and we'll sort it out.</p>
			<p><a href="/oauth/generic">Try signing in again</a></p>
		</div>
{{end}}
